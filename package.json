{
  "name": "tx.txid.uk-astro",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build && wrangler pages deploy dist --project-name=tx-txid-uk --branch=main && notify-deploy.sh tx.txid.uk"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.1",
    "@txid/ui": "file:./packages/txid-ui",
    "astro": "^6.0.5"
  }
}
